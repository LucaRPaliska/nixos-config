general {
    lock_cmd = pidof hyprlock || hyprlock   # don't spawn multiple hyprlock instances
    before_sleep_cmd = ~/.config/hypr/scripts/lock_screen.sh
    after_sleep_cmd = hyprctl dispatch dpms on
}

listener {
    timeout = 300                           # 5 min: lock screen
    on-timeout = ~/.config/hypr/scripts/lock_screen.sh
}

listener {
    timeout = 360                           # 6 min: turn off displays
    on-timeout = hyprctl dispatch dpms off
    on-resume = hyprctl dispatch dpms on
}

listener {
    timeout = 1800                          # 30 min: suspend
    on-timeout = systemctl suspend
}
